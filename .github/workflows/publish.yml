name: Publish iOS SDK (SPM & CocoaPods)

# Swift Package Manager (SPM) is decentralized and driven directly by Git tags.
# When a release/tag is published, Xcode and `swift package` resolve it via Git.
# SPM requires ZERO secrets. CocoaPods Trunk publishing runs if COCOAPODS_TRUNK_TOKEN is provided.

on:
  release:
    types: [published]
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: publish-ios-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  publish-spm:
    name: Verify Swift Package (SPM)
    runs-on: macos-14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Validate Package.swift Manifest
        run: swift package dump-package > /dev/null

      - name: Run SPM Tests
        run: swift test --enable-code-coverage

      - name: Verify Privacy Manifest
        run: |
          if [ ! -f "Sources/TaqlynSDK/PrivacyInfo.xcprivacy" ]; then
            echo "::error::Apple Required Reason PrivacyInfo.xcprivacy is missing!"
            exit 1
          fi
          echo "Privacy manifest verified."

  publish-cocoapods:
    name: Publish to CocoaPods Trunk
    needs: publish-spm
    runs-on: macos-14
    if: "${{ env.COCOAPODS_TRUNK_TOKEN != '' }}"
    env:
      COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Podspec
        run: pod ipc spec TaqlynSDK.podspec > /dev/null

      - name: Push to CocoaPods Trunk
        run: pod trunk push TaqlynSDK.podspec --allow-warnings
